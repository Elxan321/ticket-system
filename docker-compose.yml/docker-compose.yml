version: "3.9"

services:
  backend:
    build:
      context: ./cmd
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    restart: always

  frontend:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always